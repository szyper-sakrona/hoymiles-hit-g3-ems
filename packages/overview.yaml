# Szybki podgląd najważniejszych wartości

globals:
  # Local monotonic timestamps are updated exclusively by successful FC03
  # callbacks. They prevent a derived Master balance from combining a fresh
  # LOAD sample with a PV/GRID value cached before a partial Modbus outage.
  - id: overview_master_pv_last_readback_ms
    type: uint32_t
    restore_value: no
    initial_value: "0"
  - id: overview_master_grid_last_readback_ms
    type: uint32_t
    restore_value: no
    initial_value: "0"
  # The last GRID/PV pair already consumed by an aggregate generation. Both
  # source callbacks must advance before another LOAD callback may commit.
  - id: overview_parallel_aggregate_committed_grid_readback_ms
    type: uint32_t
    restore_value: no
    initial_value: "0"
  - id: overview_parallel_aggregate_committed_pv_readback_ms
    type: uint32_t
    restore_value: no
    initial_value: "0"

sensor:
  # Monotonic evidence counter for the parallel-system power balance.  It is
  # deliberately event-driven: backup_load.yaml advances it only after a real
  # Master LOAD FC03 callback can combine fresh GRID and PV replies.  A write,
  # cached template value or partial Modbus cycle must never advance it.
  - platform: template
    name: "Parallel Aggregate Power Readback Generation"
    id: parallel_aggregate_power_readback_generation
    accuracy_decimals: 0
    update_interval: never
    force_update: true
    entity_category: diagnostic

  - platform: modbus_controller
    modbus_controller_id: ${modbus_fast_controller_id}
    id: pv_total_power_master_30001
    internal: true
    register_type: read
    address: 30001
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    icon: mdi:flash
    accuracy_decimals: 0
    value_type: U_DWORD
    force_update: true
    on_value:
      then:
        - lambda: |-
            // Publish the public overview state only after this FC03 source
            // really answered.  A 1 s template timer would otherwise make an
            // old cached Modbus value look fresh forever in Home Assistant.
            const uint32_t topology_readback =
                id(parallel_topology_last_readback_ms);
            if (!id(machines_type_6048).has_state() ||
                topology_readback == 0 ||
                static_cast<uint32_t>(millis() - topology_readback) > 60000U) {
              return;
            }
            if (std::lround(id(machines_type_6048).state) != 1) {
              id(pv_total_power_30001).publish_state(x);
            }

  - platform: modbus_controller
    modbus_controller_id: ${modbus_fast_controller_id}
    name: "Overview Internal PV Total Power"
    id: internal_pv_total_power_30003
    register_type: read
    address: 30003
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    icon: mdi:flash
    accuracy_decimals: 0
    value_type: U_DWORD

  - platform: modbus_controller
    modbus_controller_id: ${modbus_fast_controller_id}
    name: "Overview External PV Total Power"
    id: external_pv_total_power_30005
    register_type: read
    address: 30005
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    icon: mdi:flash
    accuracy_decimals: 0
    value_type: U_DWORD

  - platform: modbus_controller
    modbus_controller_id: ${modbus_fast_controller_id}
    id: inv_active_power_master_30007
    internal: true
    register_type: read
    address: 30007
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    icon: mdi:flash
    accuracy_decimals: 0
    value_type: S_DWORD
    force_update: true
    on_value:
      then:
        - lambda: |-
            const uint32_t topology_readback =
                id(parallel_topology_last_readback_ms);
            if (!id(machines_type_6048).has_state() ||
                topology_readback == 0 ||
                static_cast<uint32_t>(millis() - topology_readback) > 60000U) {
              return;
            }
            if (std::lround(id(machines_type_6048).state) != 1) {
              id(inv_active_power_30007).publish_state(x);
            }

  - platform: modbus_controller
    modbus_controller_id: ${modbus_fast_controller_id}
    id: battery_power_master_30009
    internal: true
    register_type: read
    address: 30009
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    icon: mdi:flash
    accuracy_decimals: 0
    value_type: S_DWORD
    force_update: true
    on_value:
      then:
        - lambda: |-
            const uint32_t topology_readback =
                id(parallel_topology_last_readback_ms);
            if (!id(machines_type_6048).has_state() ||
                topology_readback == 0 ||
                static_cast<uint32_t>(millis() - topology_readback) > 60000U) {
              return;
            }
            if (std::lround(id(machines_type_6048).state) != 1) {
              id(battery_power_30009).publish_state(x);
            }

  - platform: modbus_controller
    modbus_controller_id: ${modbus_fast_controller_id}
    id: grid_total_active_power_master_30011
    internal: true
    register_type: read
    address: 30011
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    icon: mdi:flash
    accuracy_decimals: 0
    value_type: S_DWORD
    force_update: true
    on_value:
      then:
        - lambda: |-
            const uint32_t topology_readback =
                id(parallel_topology_last_readback_ms);
            if (!id(machines_type_6048).has_state() ||
                topology_readback == 0 ||
                static_cast<uint32_t>(millis() - topology_readback) > 60000U) {
              return;
            }
            if (std::lround(id(machines_type_6048).state) != 1) {
              id(grid_total_active_power_30011).publish_state(x);
            }

  - platform: modbus_controller
    modbus_controller_id: ${modbus_fast_controller_id}
    name: "Overview Generator Active Power"
    id: generator_active_power_30013
    register_type: read
    address: 30013
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    icon: mdi:flash
    accuracy_decimals: 0
    value_type: S_DWORD

  - platform: modbus_controller
    modbus_controller_id: ${modbus_fast_controller_id}
    id: load_active_power_master_30015
    internal: true
    register_type: read
    address: 30015
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    icon: mdi:flash
    accuracy_decimals: 0
    value_type: S_DWORD
    force_update: true
    on_value:
      then:
        - lambda: |-
            const uint32_t topology_readback =
                id(parallel_topology_last_readback_ms);
            if (!id(machines_type_6048).has_state() ||
                topology_readback == 0 ||
                static_cast<uint32_t>(millis() - topology_readback) > 60000U) {
              return;
            }
            if (std::lround(id(machines_type_6048).state) != 1) {
              id(load_active_power_30015).publish_state(x);
            }

  - platform: modbus_controller
    modbus_controller_id: ${modbus_fast_controller_id}
    name: "Overview Smart Load Active Power"
    id: smart_load_active_power_30017
    register_type: read
    address: 30017
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    icon: mdi:flash
    accuracy_decimals: 0
    # Ten model zwraca rejestr 30017 z młodszym słowem jako pierwszym.
    value_type: S_DWORD_R
    # register_count: 3

  - platform: modbus_controller
    modbus_controller_id: ${modbus_fast_controller_id}
    id: battery_soc_master_30020
    internal: true
    register_type: read
    address: 30020
    reuse_previous_range: true
    unit_of_measurement: "%"
    device_class: battery
    state_class: measurement
    accuracy_decimals: 0
    value_type: U_WORD
    force_update: true
    on_value:
      then:
        - lambda: |-
            const uint32_t topology_readback =
                id(parallel_topology_last_readback_ms);
            if (!id(machines_type_6048).has_state() ||
                topology_readback == 0 ||
                static_cast<uint32_t>(millis() - topology_readback) > 60000U) {
              return;
            }
            if (std::lround(id(machines_type_6048).state) != 1) {
              id(battery_soc_30020).publish_state(x);
            }

  # Encje poniżej zachowują dotychczasowe nazwy i identyfikatory Home
  # Assistant. Dla pojedynczego falownika publikują szybki blok 30001.
  # W trybie równoległym korzystają z wartości całego systemu udostępnianych
  # przez Mastera w blokach energii i licznika. Rejestry systemowe PV (2150)
  # i LOAD (2169) są typu U_WORD, więc ich zakres kończy się na 65 535 W.
  # Układy przekraczające ten zakres wymagają potwierdzonego systemowego
  # źródła 32-bitowego; sam U_WORD nie pozwala wykryć zawinięcia do zera.
  - platform: template
    name: "Overview PV Total Power"
    id: pv_total_power_30001
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    icon: mdi:flash
    accuracy_decimals: 0
    # This sensor is event-driven by the selected physical FC03 source below.
    # Its HA timestamp therefore describes a real poll, not a cached republish.
    update_interval: never
    force_update: true

  - platform: template
    name: "Overview Inverter Active Power"
    id: inv_active_power_30007
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    icon: mdi:flash
    accuracy_decimals: 0
    update_interval: never
    force_update: true

  - platform: template
    name: "Overview Battery Power"
    id: battery_power_30009
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    icon: mdi:flash
    accuracy_decimals: 0
    update_interval: never
    force_update: true

  - platform: template
    name: "Overview Grid Total Active Power"
    id: grid_total_active_power_30011
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    icon: mdi:flash
    accuracy_decimals: 0
    update_interval: never
    force_update: true

  - platform: template
    name: "Overview Load Active Power"
    id: load_active_power_30015
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    icon: mdi:flash
    accuracy_decimals: 0
    update_interval: never
    force_update: true

  - platform: template
    name: "Overview Battery SOC"
    id: battery_soc_30020
    unit_of_measurement: "%"
    device_class: battery
    state_class: measurement
    accuracy_decimals: 0
    update_interval: never
    force_update: true
